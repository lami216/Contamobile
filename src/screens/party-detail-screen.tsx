import { useCallback, useMemo, useState } from 'react';
import { Alert, FlatList, Pressable, StyleSheet, View } from 'react-native';
import { router, useFocusEffect, useLocalSearchParams } from 'expo-router';
import { useSQLiteContext } from 'expo-sqlite';
import type { DocumentRecord, Party, PaymentAccount } from '@/domain/types';
import { getParty, getPartyFinancialSummary, listPaymentAccounts, type PartyFinancialSummary } from '@/db/queries';
import { listDocumentHeaders } from '@/db/document-queries';
import { postPartyCash } from '@/services/accounting-service';
import { updateParty } from '@/services/management-service';
import { postOffset, postSettlement } from '@/services/party-ledger-service';
import { AppText, Badge, Button, Card, Chip, EmptyState, Field, Money, Screen, SectionTitle } from '@/components/ui';
import { CompactMetric, Sheet } from '@/components/mobile-interactions';
import { useI18n } from '@/i18n/provider';
import { useAuth } from '@/auth/provider';
import { colors, radius, spacing } from '@/theme';

type Action='receive'|'pay'|'settlement'|'offset'|'edit'|null;
type Period='today'|'all'|'custom';
const emptySummary=(partyId=''):PartyFinancialSummary=>({partyId,cashIn:0,cashOut:0,customerTradeTotal:0,customerGrossProfit:0,supplierTradeTotal:0,supplierInvoiceCount:0});
function localDay(){const value=new Date(),y=value.getFullYear(),m=String(value.getMonth()+1).padStart(2,'0'),d=String(value.getDate()).padStart(2,'0');return `${y}-${m}-${d}`}

export function PartyDetailScreen(){
  const {id}=useLocalSearchParams<{id:string}>(),db=useSQLiteContext(),{t,date,isRTL,locale,number,errorMessage}=useI18n(),auth=useAuth(),ar=locale==='ar',today=localDay();
  const [party,setParty]=useState<Party|null>(null),[docs,setDocs]=useState<DocumentRecord[]>([]),[accounts,setAccounts]=useState<PaymentAccount[]>([]),[summary,setSummary]=useState<PartyFinancialSummary>(()=>emptySummary(id));
  const [action,setAction]=useState<Action>(null),[period,setPeriod]=useState<Period>('today'),[from,setFrom]=useState(today),[to,setTo]=useState(today),[dateSheet,setDateSheet]=useState(false),[busy,setBusy]=useState(false);
  const load=useCallback(async()=>{if(!id)return;const range=period==='today'?{from:today,to:today}:period==='custom'?{from:from||undefined,to:to||undefined}:{from:undefined,to:undefined};const [p,d,a,s]=await Promise.all([getParty(db,id),listDocumentHeaders(db,{partyId:id,...range,limit:150}),listPaymentAccounts(db),getPartyFinancialSummary(db,id)]);setParty(p);setDocs(d);setAccounts(a.filter(account=>account.isActive&&!account.isArchived));setSummary(s)},[db,from,id,period,to,today]);
  useFocusEffect(useCallback(()=>{void load()},[load]));
  if(!party)return <Screen><Button title={t('cancel')} variant="ghost" onPress={()=>router.back()}/><EmptyState title={t('noData')}/></Screen>;
  const customer=party.partyType==='customer';
  const canView=auth.has(customer?'customers.view':'suppliers.view'),canMove=auth.has(customer?'customers.collect':'suppliers.pay'),canEdit=auth.has(customer?'customers.edit':'suppliers.edit'),canLedger=canEdit;
  if(!canView)return <Screen><EmptyState title={ar?'ليس لديك صلاحية عرض هذا الحساب':'Vous n’avez pas accès à ce compte.'}/><Button title={t('cancel')} variant="ghost" onPress={()=>router.back()}/></Screen>;
  const trade=customer?summary.customerTradeTotal:summary.supplierTradeTotal;
  const cash=customer?summary.cashIn:summary.cashOut;
  const balanceTitle=party.net>0?(ar?'مستحق لنا':'À recevoir'):party.net<0?(ar?'مستحق علينا':'À payer'):(ar?'الحساب مسدد':'Compte soldé');
  const balanceTone=party.net>0?'positive':party.net<0?'negative':'normal';
  const run=async(task:()=>Promise<unknown>)=>{if(busy)return;setBusy(true);try{await task();setAction(null);await load()}catch(error){Alert.alert(t('error'),errorMessage(error))}finally{setBusy(false)}};
  const periodLabel=period==='today'?(ar?'اليوم':'Aujourd’hui'):period==='all'?(ar?'كل المدة':'Toute la période'):`${from} → ${to}`;
  return <Screen padded={false}><FlatList data={docs} keyExtractor={item=>item.id} contentContainerStyle={styles.list} ListHeaderComponent={<View style={styles.header}>
    <SectionTitle title={party.name} subtitle={party.phone||undefined} action={<Button compact title={t('cancel')} variant="ghost" onPress={()=>router.back()}/>}/>
    <Card tone="primary" style={styles.balanceHero}><View style={[styles.balanceTop,{flexDirection:isRTL?'row-reverse':'row'}]}><View style={styles.flex}><Badge label={customer?t('customer'):t('supplier')} tone="primary"/><AppText variant="caption" muted>{balanceTitle}</AppText><Money value={Math.abs(party.net)} tone={balanceTone} large/><AppText variant="caption" muted>{ar?`لنا ${number(party.receivable)} • علينا ${number(party.payable)}`:`À recevoir ${number(party.receivable)} • à payer ${number(party.payable)}`}</AppText></View>{canEdit?<Button compact title={t('edit')} variant="ghost" onPress={()=>setAction('edit')}/>:null}</View></Card>
    {(canMove||canLedger)?<View style={[styles.actions,{flexDirection:isRTL?'row-reverse':'row'}]}>{canMove?<><Button title={t('receive')} onPress={()=>setAction('receive')}/><Button title={t('pay')} variant="secondary" onPress={()=>setAction('pay')}/></>:null}{canLedger&&party.net!==0?<Button title={ar?'تسوية':'Règlement'} variant="secondary" onPress={()=>setAction('settlement')}/>:null}{canLedger&&party.receivable>0&&party.payable>0?<Button title={ar?'مقاصة':'Compensation'} variant="secondary" onPress={()=>setAction('offset')}/>:null}</View>:null}
    <View style={[styles.metrics,{flexDirection:isRTL?'row-reverse':'row'}]}><CompactMetric label={customer?(ar?'إجمالي مشترياته':'Achats du client'):(ar?'مشترياتنا منه':'Nos achats')} value={trade}/><CompactMetric label={customer?(ar?'المبالغ المحصلة':'Encaissements'):(ar?'المبالغ المدفوعة':'Paiements')} value={cash}/>{customer?<CompactMetric label={ar?'الربح الإجمالي':'Bénéfice brut'} value={summary.customerGrossProfit} tone={summary.customerGrossProfit>=0?'positive':'negative'}/>:<View style={styles.countMetric}><AppText variant="caption" muted>{ar?'فواتير الشراء':'Factures'}</AppText><AppText variant="heading">{number(summary.supplierInvoiceCount)}</AppText></View>}</View>
    <SectionTitle title={t('records')} subtitle={ar?`الفترة: ${periodLabel}`:`Période : ${periodLabel}`}/><View style={[styles.periods,{flexDirection:isRTL?'row-reverse':'row'}]}><Chip label={ar?'اليوم':'Aujourd’hui'} active={period==='today'} onPress={()=>setPeriod('today')}/><Chip label={ar?'كل المدة':'Toute la période'} active={period==='all'} onPress={()=>setPeriod('all')}/><Button compact title={ar?'فترة':'Période'} variant={period==='custom'?'secondary':'ghost'} onPress={()=>setDateSheet(true)}/></View>
  </View>} ListEmptyComponent={<EmptyState title={t('noData')} description={ar?'لا توجد حركات في الفترة المحددة.':'Aucune opération sur cette période.'}/>} renderItem={({item})=>{
    const label=item.kind==='sale'?(ar?'بيع':'Vente'):item.kind==='purchase'?(ar?'شراء':'Achat'):item.kind==='expense'?(ar?'مصروف':'Dépense'):item.kind==='payment'?(ar?'دفع/تحصيل':'Paiement'):item.kind==='settlement'?(ar?'تسوية':'Règlement'):item.kind==='offset'?(ar?'مقاصة':'Compensation'):(ar?'حركة':'Mouvement');
    return <View style={[styles.row,{flexDirection:isRTL?'row-reverse':'row'}]}><View style={styles.flex}><View style={[styles.rowTop,{flexDirection:isRTL?'row-reverse':'row'}]}><Badge label={label} tone={item.kind==='sale'?'positive':item.kind==='purchase'?'warning':'neutral'}/><AppText variant="caption" muted>{date(item.occurredAt)}</AppText></View><AppText variant="subheading" numberOfLines={1}>{item.title??item.number}</AppText><AppText variant="caption" muted>{item.number}</AppText></View><Money value={item.total}/></View>;
  }}/>

  <Sheet visible={action==='edit'} title={ar?'تعديل الحساب':'Modifier le compte'} onClose={()=>{if(!busy)setAction(null)}}>{action==='edit'?<EditPartyForm party={party} busy={busy} onCancel={()=>setAction(null)} onSave={(name,phone)=>void run(()=>updateParty(db,party.id,{name,phone}))}/>:null}</Sheet>
  <Sheet visible={action==='receive'||action==='pay'} title={action==='receive'?t('receive'):t('pay')} onClose={()=>{if(!busy)setAction(null)}}>{action==='receive'||action==='pay'?<CashForm direction={action} accounts={accounts} busy={busy} onCancel={()=>setAction(null)} onSave={(amount,method,note)=>void run(()=>postPartyCash(db,{partyId:party.id,direction:action,amount,paymentMethod:method,note}))}/>:null}</Sheet>
  <Sheet visible={action==='settlement'||action==='offset'} title={action==='offset'?(ar?'مقاصة':'Compensation'):(ar?'تسوية محاسبية':'Règlement comptable')} onClose={()=>{if(!busy)setAction(null)}}>{action==='settlement'||action==='offset'?<LedgerForm mode={action} party={party} busy={busy} onCancel={()=>setAction(null)} onSave={(amount,note)=>void run(()=>action==='offset'?postOffset(db,{partyId:party.id,amount,note}):postSettlement(db,{partyId:party.id,side:party.receivable>0?'receivable':'payable',amount,note}))}/>:null}</Sheet>
  <Sheet visible={dateSheet} title={ar?'فترة السجل':'Période de l’historique'} onClose={()=>setDateSheet(false)} footer={<><Button title={t('confirm')} disabled={!from||!to} onPress={()=>{setPeriod('custom');setDateSheet(false)}}/><Button title={t('cancel')} variant="ghost" onPress={()=>setDateSheet(false)}/></>}><View style={[styles.datePair,{flexDirection:isRTL?'row-reverse':'row'}]}><Field label={t('from')} value={from} onChangeText={setFrom} placeholder="YYYY-MM-DD" containerStyle={styles.flex}/><Field label={t('to')} value={to} onChangeText={setTo} placeholder="YYYY-MM-DD" containerStyle={styles.flex}/></View></Sheet>
  </Screen>;
}

function EditPartyForm({party,busy,onCancel,onSave}:{party:Party;busy:boolean;onCancel:()=>void;onSave:(name:string,phone:string)=>void}){const {t}=useI18n();const [name,setName]=useState(party.name),[phone,setPhone]=useState(party.phone);return <View style={styles.form}><Field label={t('name')} value={name} onChangeText={setName} autoFocus/><Field label={t('phone')} keyboardType="phone-pad" value={phone} onChangeText={setPhone}/><Button title={t('save')} loading={busy} disabled={!name.trim()} onPress={()=>onSave(name.trim(),phone)}/><Button title={t('cancel')} variant="ghost" disabled={busy} onPress={onCancel}/></View>}
function CashForm({direction,accounts,busy,onCancel,onSave}:{direction:'receive'|'pay';accounts:PaymentAccount[];busy:boolean;onCancel:()=>void;onSave:(amount:number,method:string,note:string)=>void}){const {t,locale,isRTL}=useI18n(),ar=locale==='ar';const [amount,setAmount]=useState(''),[method,setMethod]=useState(accounts.find(a=>a.code==='cash')?.id??accounts[0]?.id??''),[note,setNote]=useState('');const value=Number(amount),valid=Number.isFinite(value)&&value>0&&method;return <View style={styles.form}><Field label={t('amount')} value={amount} keyboardType="number-pad" onChangeText={setAmount} autoFocus/><View style={styles.form}><AppText variant="caption" muted>{t('paymentMethod')}</AppText>{accounts.length?<View style={[styles.chips,{flexDirection:isRTL?'row-reverse':'row'}]}>{accounts.map(account=><Chip key={account.id} label={account.name} active={method===account.id} onPress={()=>setMethod(account.id)}/>)}</View>:<Card tone="warning"><AppText variant="caption" style={styles.warning}>{ar?'لا توجد وسيلة دفع نشطة.':'Aucun moyen de paiement actif.'}</AppText></Card>}</View><Field label={t('note')} value={note} onChangeText={setNote}/><Button title={direction==='receive'?t('receive'):t('pay')} loading={busy} disabled={!valid} onPress={()=>onSave(value,method,note)}/><Button title={t('cancel')} variant="ghost" disabled={busy} onPress={onCancel}/></View>}
function LedgerForm({mode,party,busy,onCancel,onSave}:{mode:'settlement'|'offset';party:Party;busy:boolean;onCancel:()=>void;onSave:(amount:number,note:string)=>void}){const {t,locale}=useI18n(),ar=locale==='ar';const maximum=mode==='offset'?Math.min(party.receivable,party.payable):party.receivable>0?party.receivable:party.payable;const [amount,setAmount]=useState(String(maximum)),[note,setNote]=useState('');const value=Number(amount),valid=Number.isFinite(value)&&value>0&&value<=maximum;return <View style={styles.form}><Card tone="muted"><AppText variant="caption" muted>{mode==='offset'?(ar?'المقاصة تقلل المستحق لنا والمستحق علينا بنفس القيمة دون حركة نقدية.':'La compensation réduit les deux soldes sans mouvement de trésorerie.'):(ar?'التسوية تعدّل الرصيد المحاسبي دون حركة نقدية.':'Le règlement ajuste le solde sans mouvement de trésorerie.')}</AppText></Card><AppText variant="caption" muted>{ar?`الحد الأقصى ${maximum}`:`Maximum ${maximum}`}</AppText><Field label={t('amount')} value={amount} keyboardType="number-pad" onChangeText={setAmount}/><Field label={t('note')} value={note} onChangeText={setNote}/><Button title={t('confirm')} loading={busy} disabled={!valid} onPress={()=>onSave(value,note)}/><Button title={t('cancel')} variant="ghost" disabled={busy} onPress={onCancel}/></View>}

const styles=StyleSheet.create({
  list:{padding:spacing.md,paddingBottom:spacing.xxl,backgroundColor:colors.background},header:{gap:spacing.md,marginBottom:spacing.sm},flex:{flex:1},
  balanceHero:{shadowOpacity:0,elevation:0},balanceTop:{alignItems:'flex-start',gap:spacing.md},actions:{gap:spacing.sm,flexWrap:'wrap'},metrics:{gap:spacing.sm,flexWrap:'wrap'},countMetric:{flex:1,minWidth:132,borderRadius:radius.lg,backgroundColor:colors.surface,padding:spacing.md,gap:spacing.xs,borderWidth:1,borderColor:colors.border},periods:{alignItems:'center',gap:spacing.xs,flexWrap:'wrap'},
  row:{minHeight:86,alignItems:'center',gap:spacing.md,paddingVertical:spacing.md,borderBottomWidth:StyleSheet.hairlineWidth,borderBottomColor:colors.border},rowTop:{alignItems:'center',gap:spacing.xs,flexWrap:'wrap'},
  form:{gap:spacing.md},chips:{flexWrap:'wrap',gap:spacing.xs},warning:{color:colors.warning,fontWeight:'700'},datePair:{gap:spacing.sm},
});
